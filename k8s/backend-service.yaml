apiVersion: v1
kind: Service
metadata:
  name: tienda-backend
  namespace: tienda
spec:
  type: ClusterIP
  selector:
    app: tienda-backend
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: alt-port
      port: 8080
      targetPort: 8080